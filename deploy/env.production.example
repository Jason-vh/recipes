# Written by .github/workflows/deploy.yml from GitHub Actions secrets.
# This file shows the shape only — never commit real values.

APP_URL=https://recipes.vhtm.eu
APP_SECRET=replace-me
DATABASE_URL=postgresql://recipes:replace-me@postgres:5432/recipes
RECIPES_APP_HOST_PORT=3002
COMPOSE_PROJECT_NAME=recipes
